name: CI

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]
  # The Release workflow runs these tests before publishing.
  workflow_call:

permissions:
  contents: read

jobs:
  test:
    name: Test (Java 8)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7

      - name: Install devcontainer CLI
        run: npm install -g @devcontainers/cli@0

      - name: Test and package in dev container
        env:
          JAVA_VERSION: "8"
        run: |
          devcontainer up --workspace-folder .
          devcontainer exec --workspace-folder . bash -lc "./mvnw -B package"
